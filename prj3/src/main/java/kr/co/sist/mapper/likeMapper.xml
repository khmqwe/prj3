<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.sist.likeMapper">
  
	<select id="selectLike" resultType="kr.co.sist.domain.LikeCountDomain">
		select tr_num, likeCnt
		from	(select tr_num, count(tr_num) likeCnt
				from LIKES
				group by tr_num
				order by likeCnt desc)
		where rownum &lt;= 4
	</select>
  
</mapper>